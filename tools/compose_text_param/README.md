# CI smoke fixture

This wrapper is retained from `galaxyproject/tools-iuc` as a lightweight,
dependency-free fixture for validating the complete tools-iwc-lab pull-request
workflow. It is not intended to diverge from or replace the upstream wrapper.

